import React, { FC } from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { IconProps } from 'react-native-vector-icons/Icon';

export const MaterialIcons: FC<IconProps> = (props) => <Icon {...props} />;
